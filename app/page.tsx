export default function Home() {
  return (
    <main className="starter-shell">
      <section className="starter-panel" aria-labelledby="starter-title">
        <p className="starter-label">TMWC Youth Site</p>
        <h1 id="starter-title">Ready to rebuild</h1>
        <p>
          The previous interfaces, flows, and routes have been cleared. The
          Render, Supabase, database, and project tooling foundation remains in
          place.
        </p>
      </section>
    </main>
  );
}
